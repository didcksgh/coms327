This assignment is for reading data from CSV files and storing the data.
We will parse the follwoing data:
    pokemon.CSV
    moves.CSV
    pokemon_moves.CSV
    pokemon_species.CSV
    experience.CSV
    type_names.CSV
    pokemon_stats.CSV
    stats.CSV
    pokemon_types.CSV

The game is still compiled and run but it is just terminated immediately.
